# ImapSync-With-UI
IMAPSync With Modern UI for HestiaCP (Ubuntu 24.04) — installs imapsync server-wide and deploys a clean, responsive web UI on a Hestia domain/subdomain so your users/clients can run mailbox migrations securely using their Hestia username + password or Access/Secret Key.
